{
  "name": "interceptor",
  "version": "1.0.0",
  "description": "A Chrome devtools extension for intercepting and modifying HTTP requests",
  "main": "out/index.js",
  "scripts": {
    "build": "esbuild src/devtools-background.ts src/index.tsx --bundle --minify --outdir=out",
    "watch": "esbuild src/devtools-background.ts src/index.tsx --bundle --sourcemap=inline --outdir=out --watch"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/chrome": "^0.0.177",
    "@types/react": "^17.0.38",
    "@types/react-dom": "^17.0.11",
    "esbuild": "^0.14.14",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
